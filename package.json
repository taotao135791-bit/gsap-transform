{
  "name": "gsap-transform",
  "version": "0.3.0",
  "description": "Community, third-party GSAP motion skills for agent-native timeline editing via primitives + state.json.",
  "type": "module",
  "scripts": {
    "test": "node --test 'tests/**/*.test.mjs'",
    "verify": "node scripts/verify-consistency.mjs",
    "gen:templates": "node scripts/gen-templates.mjs",
    "pick": "node scripts/pick-template.mjs",
    "studio:gen": "node scripts/state-to-scene.mjs"
  },
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
